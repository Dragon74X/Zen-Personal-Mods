[
  {
    "type": "separator",
    "label": "Tab Router",
    "size": "20px",
    "margin": "2px 0 12px 0"
  },
  {
    "type": "checkbox",
    "property": "zzrouter.enabled",
    "label": "Turn on routing *Moves tabs into tab groups automatically as you browse, using the rules below. Nothing moves until you write at least one rule.*",
    "defaultValue": false
  },
  {
    "type": "string",
    "property": "zzrouter.folder-separator",
    "label": "Subgroup separator *Splits a group name into nesting levels. With the default, Work / Email / Gmail creates a group three levels deep -- real nested tab groups, never pinned. Include the spaces if you want them; '/' with no spaces works too.*",
    "defaultValue": " / ",
    "placeholder": " / "
  },
  {
    "type": "dropdown",
    "value": "number",
    "property": "zzrouter.max-depth",
    "label": "Nesting limit *Hard cap on how deep automatic filing goes, counting the top group as level one. Two gives domain plus one subgroup. A safety valve: with Unlimited the mod already refuses to build repeated back-and-forth nesting.*",
    "defaultValue": 0,
    "options": [
      {
        "label": "Unlimited",
        "value": 0
      },
      {
        "label": "One level (flat)",
        "value": 1
      },
      {
        "label": "Two levels",
        "value": 2
      },
      {
        "label": "Three levels",
        "value": 3
      }
    ]
  },
  {
    "type": "string",
    "property": "zzrouter.rules",
    "label": "Rules *One per line, or separated by semicolons. Left of the > is a comma separated list of domains, right of it is the group name. Subdomains match automatically, so github.com also catches gist.github.com. First match wins, so put narrower rules first. You do not have to write these by hand: open the Browser Console and run TabRouter.suggestRules() to generate a block from the tabs you already have open, or TabRouter.suggestRules({subdomains:true}) to get a nested Parent / Child rule per subdomain. It copies to your clipboard and moves nothing.*",
    "defaultValue": "github.com, gitlab.com > Dev ; youtube.com > Watch",
    "placeholder": "github.com, gitlab.com > Dev ; youtube.com > Watch"
  },
  {
    "type": "checkbox",
    "property": "zzrouter.auto-unmatched",
    "label": "Sort everything automatically *Derives the group name from the site itself, so a page from somewhere you have never been still gets filed without any rule existing for it. Rules still win where they match, so use them to override the automatic name. This is the switch that makes the mod work with no setup.*",
    "defaultValue": true
  },
  {
    "type": "checkbox",
    "property": "zzrouter.auto-subdomains",
    "label": "Use subdomains as subgroups *docs.proton.me becomes a Docs subgroup nested inside Proton, rather than just Proton. www is ignored, since it is noise rather than a real subdomain.*",
    "defaultValue": true
  },
  {
    "type": "dropdown",
    "value": "number",
    "property": "zzrouter.auto-depth",
    "label": "How many subdomain levels *How deep to nest for hosts like a.b.example.com. One level gives Example / B. Two gives Example / B / A. Most sites never need more than one.*",
    "defaultValue": 1,
    "options": [
      {
        "label": "One level",
        "value": 1
      },
      {
        "label": "Two levels",
        "value": 2
      },
      {
        "label": "Three levels",
        "value": 3
      }
    ]
  },
  {
    "type": "dropdown",
    "value": "number",
    "property": "zzrouter.auto-path-depth",
    "label": "Use URL path as subgroups *Subdomains cannot split a site that separates sections by path. nexusmods.com/games/cyberpunk2077 has NO subdomain -- www is stripped -- so every game lands in one group no matter how deep subdomain nesting goes. This reads path segments instead. One level on that URL gives a Cyberpunk 2077 subgroup inside Nexusmods, since 'games' is filtered as a generic word.*",
    "defaultValue": 0,
    "options": [
      {
        "label": "Off",
        "value": 0
      },
      {
        "label": "One level",
        "value": 1
      },
      {
        "label": "Two levels",
        "value": 2
      },
      {
        "label": "Three levels",
        "value": 3
      }
    ]
  },
  {
    "type": "string",
    "property": "zzrouter.auto-path-ignore",
    "label": "Extra path words to ignore *Comma separated, ADDED to a built-in list of generic route words (search, watch, videos, results, games, category and similar). Numeric segments and file names are always dropped.*",
    "defaultValue": "",
    "placeholder": "mods,collections"
  },
  {
    "type": "string",
    "property": "zzrouter.auto-path-aliases",
    "label": "Path name aliases *slug = Pretty Name, one per line or comma separated, and these always win. Mostly optional now: names are LEARNED automatically from tab titles when the title plausibly matches the slug, so nexusmods.com/games/dragonage files as Dragon Age: Origins once any page under it has loaded. TabRouter.learned() shows what has been picked up; TabRouter.forget('slug') or TabRouter.forget() drops bad ones. Slugs with separators, like crimson-desert, are cleaned up automatically either way.*",
    "defaultValue": "baldursgate3 = Baldur's Gate 3",
    "placeholder": "baldursgate3 = Baldur's Gate 3"
  },
  {
    "type": "checkbox",
    "property": "zzrouter.create-groups",
    "label": "Create groups that do not exist yet *Off means tabs are only moved into groups you already made by hand, and rules naming a missing group do nothing.*",
    "defaultValue": true
  },
  {
    "type": "separator",
    "label": "Leave alone",
    "size": "17px",
    "margin": "22px 0 10px 0"
  },
  {
    "type": "checkbox",
    "property": "zzrouter.skip-grouped",
    "label": "Tabs already in a group *The important one. On, routing only ever touches loose tabs, so anything you filed by hand stays put. Turn it off only if you want rules to override your own sorting.*",
    "defaultValue": true
  },
  {
    "type": "checkbox",
    "property": "zzrouter.refile-mismatched",
    "label": "Re-file tabs that drifted *A link opened from a grouped tab inherits that group even when it goes somewhere unrelated, which is why a stray domain stays stuck in the group it was opened from. With this on, a tab whose group no longer matches where it belongs gets moved. Only applies when tabs already in a group are otherwise skipped.*",
    "defaultValue": true
  },
  {
    "type": "checkbox",
    "property": "zzrouter.skip-essentials",
    "label": "Essentials *Essentials are pinned deliberately and are not loose tabs.*",
    "defaultValue": true
  },
  {
    "type": "checkbox",
    "property": "zzrouter.skip-pinned",
    "label": "Pinned tabs *Separate from Essentials.*",
    "defaultValue": true
  },
  {
    "type": "separator",
    "label": "Timing",
    "size": "17px",
    "margin": "22px 0 10px 0"
  },
  {
    "type": "checkbox",
    "property": "zzrouter.sort-on-startup",
    "label": "Sort everything at startup *Runs one pass a few seconds after the window opens. Off by default, since a session restore moving dozens of tabs at once is jarring.*",
    "defaultValue": false
  },
  {
    "type": "string",
    "property": "zzrouter.delay-ms",
    "label": "Wait before routing (milliseconds) *How long to wait after a page loads before moving its tab. A short delay avoids chasing redirects, which would otherwise file a tab under the redirector instead of the destination.*",
    "defaultValue": "400",
    "placeholder": "400"
  },
  {
    "type": "separator",
    "label": "Troubleshooting",
    "size": "17px",
    "margin": "22px 0 10px 0"
  },
  {
    "type": "checkbox",
    "property": "zzrouter.debug",
    "label": "Log what it is doing *Prints to the Browser Console (Ctrl+Shift+J) under [TabRouter]. Also gives you TabRouter.preview(), which lists every tab with the group it would go to without moving anything, and TabRouter.sortAll() to run one pass on demand.*",
    "defaultValue": false
  }
]